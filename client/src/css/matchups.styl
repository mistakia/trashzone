@import 'nib'
@import 'variables.styl'

#matchups
    border-top 1px solid $section-border
    border-bottom 1px solid $section-border
    text-align center
    .matchup
        margin-bottom 10px
    span
        font-size 0.5rem
        line-height 0.6rem