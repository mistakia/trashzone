{
  "name": "trashzone",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "async": "^2.6.4",
    "cheerio": "^1.0.0-rc.2",
    "espnff": "github:mistakia/espnff",
    "express": "^4.15.4",
    "gaussian": "^1.1.0",
    "glob": "^7.1.3",
    "jsonfile": "^4.0.0",
    "logplease": "^1.2.14",
    "machine": "github:trashman-industries/machine",
    "moment": "^2.18.1",
    "request": "^2.81.0"
  },
  "devDependencies": {
    "groupme": "^0.5.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:draft && npm run build:home && npm run build:league && npm run build:odds && npm run build:rankings && npm run build:waivers",
    "build:draft": "cd src/draft && grunt",
    "build:home": "cd src/home && grunt",
    "build:league": "cd src/league && grunt",
    "build:odds": "cd src/odds && grunt",
    "build:rankings": "cd src/rankings && grunt",
    "build:waivers": "cd src/odds && grunt"
  },
  "author": "",
  "license": "MIT"
}
